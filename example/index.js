const tester = require("../");

example.describe("Running some example tests", example => {

    example.it("should be able to add instances", () => {
        this.expect(true).toBe(true);
    });

    example.it("should be able to add instances", () => {
        this.expect(true).toBe(true);
    });
});
